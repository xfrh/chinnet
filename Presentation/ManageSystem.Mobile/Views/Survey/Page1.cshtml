
@{
    ViewBag.Title = "问卷";
    Layout = "~/Views/Shared/_SurveyLayout.cshtml";
    string _version = DateTime.Now.Ticks.ToString();
}

@section Styles{
    <link href="~/Content/Css/Survey/Page1.css?v=@_version" rel="stylesheet" />
}
@section Scripts{
    <script src="~/Content/Scripts/Survey/Page1.js?v=@_version"></script>
}

<div id="app" v-locak>
    <input id="hidden_id" name="hidden_id" ref="hidden_id" type="hidden" hidden="hidden" value="@ViewContext.RouteData.Values["id"]" />
    <input id="hidden_datetime" name="hidden_datetime" ref="hidden_datetime" type="hidden" hidden="hidden" value="@DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss")" />
    <div id="page" class="swiper-container">
        <div class="swiper-wrapper">
            <div class="swiper-slide slidescroll">
                <div class="survey-body">
                    <article class="cro-page">
                        <div class="cro-qbox">
                            <div class="cro-qbox-title" v-html="Q1.title"></div>
                            <div :class="{'cro-qbox-awbox': true, 'has-error': errors.has(Q1.name)}">
                                <input class="cro-qbox__input" :name="Q1.name" v-model="Q1.answer" v-validate="{required: true, decimal: 0}" type="tel" :placeholder="Q1.placeholder" @@blur="onBlur" />
                            </div>
                        </div>
                        <div class="cro-qbox">
                            <div class="cro-qbox-title" v-html="Q2.title"></div>
                            <div :class="{'cro-qbox-awbox': true, 'has-error': errors.has(Q2.name)}">
                                <input class="cro-qbox__input" :name="Q2.name" v-model="Q2.answer" v-validate="{required: true, decimal: 0}" type="tel" :placeholder="Q1.placeholder" @@blur="onBlur" />
                            </div>
                        </div>
                    </article>
                </div>
                <footer class="cre-footer">
                    <a class="btn btn-primary cre-submit a-btn" href="javascript: void(0);" @@click="onSubmit">下一页</a>
                </footer>
            </div>
        </div>
    </div>
</div>