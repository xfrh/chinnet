
@{
    ViewBag.Title = "问卷";
    Layout = "~/Views/Shared/_SurveyLayout.cshtml";
    string _version = DateTime.Now.Ticks.ToString();
}

@section Styles{
    <link href="~/Content/Css/Survey/Page6.css?v=@_version" rel="stylesheet" />
}
@section Scripts{
    <script src="~/Content/Scripts/Survey/Page6.js?v=@_version"></script>
}

<div id="app" v-locak>
    <input id="hidden_id" name="hidden_id" ref="hidden_id" type="hidden" hidden="hidden" value="@ViewContext.RouteData.Values["id"]" />
    <div id="page" class="swiper-container">
        <div class="swiper-wrapper">
            <div class="swiper-slide slidescroll">
                <div class="survey-body">
                    <article class="cro-page">
                        <div class="cro-qbox">
                            <div class="cro-qbox-title" v-html="Q11.title"></div>
                            <div class="cro-qbox-awbox">
                                <div v-for="(item,index) in Q11.options" :class="{'flex': true, 'has-error': errors.has(item.name)}">
                                    <div class="w20">
                                        <div class="checkbox-control">
                                            <input :id="item.id" v-model="Q11.answer" class="checkbox-control-input" type="checkbox" :name="item.name" :value="item.value" v-validate="{required: true, length: [1, 3]}" v-if="index === 0" />
                                            <input :id="item.id" v-model="Q11.answer" class="checkbox-control-input" type="checkbox" :name="item.name" :value="item.value" v-if="index > 0" />
                                            <label :for="item.id" class="checkbox-control-label"></label>
                                        </div>
                                    </div>
                                    <div class="flex-1">
                                        <div class="" style="padding-top:2px;">
                                            <label :for="item.id" v-text="item.text" v-if="item.value !== '其他'"></label>
                                            <div v-if="item.value === '其他'" :class="{'flex cro-qbox-awbox__other': true, 'has-error': errors.has('Q11_other')}">
                                                <label :for="item.id" style="width: 3rem;" v-text="item.text"></label>
                                                <input class="flex-1 cro-qbox__input font-14px" name="Q11_other" v-model="Q11.other" v-validate="{required: Q11.answer.includes('其他'), min: 1}" type="text" />
                                            </div>
                                        </div>
                                        <div class="flex align-items__baseline" v-if="Q11.answer.indexOf(item.value) >= 0">
                                            <div class="font-12px">百分比：</div>
                                            <div :class="{'cro-qbox-awbox flex-1': true, 'has-error': errors.has(item.rate_name)}">
                                                <input class="cro-qbox__input font-14px" :name="item.rate_name" v-model="item.rate" v-validate="{required: true}" type="text" placeholder="请选择百分比" style="background-color: transparent;" readonly="readonly" @@click="onQ11Picker($event,item)" />
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="cro-qbox">
                            <div class="cro-qbox-title" v-html="Q12.title"></div>
                            <div class="cro-qbox-awbox">
                                <div v-for="(item,index) in Q12.options" :class="{'flex': true, 'has-error': errors.has(item.name)}">
                                    <div class="w20">
                                        <div class="checkbox-control">
                                            <input :id="item.id" v-model="Q12.answer" class="checkbox-control-input" type="checkbox" :name="item.name" :value="item.value" v-validate="{required: true, length: [1, 3]}" v-if="index === 0" />
                                            <input :id="item.id" v-model="Q12.answer" class="checkbox-control-input" type="checkbox" :name="item.name" :value="item.value" v-if="index > 0" />
                                            <label :for="item.id" class="checkbox-control-label"></label>
                                        </div>
                                    </div>
                                    <div class="flex-1">
                                        <div class="" style="padding-top:2px;">
                                            <label :for="item.id" v-text="item.text" v-if="item.value !== '其他'"></label>
                                            <div v-if="item.value === '其他'" :class="{'flex cro-qbox-awbox__other': true, 'has-error': errors.has('Q12_other')}">
                                                <label :for="item.id" style="width: 3rem;" v-text="item.text"></label>
                                                <input class="flex-1 cro-qbox__input font-14px" name="Q12_other" v-model="Q12.other" v-validate="{required: Q12.answer.includes('其他'), min: 1}" type="text" />
                                            </div>
                                        </div>
                                        <div class="flex align-items__baseline" v-if="Q12.answer.includes(item.value)">
                                            <div class="font-12px">百分比：</div>
                                            <div :class="{'cro-qbox-awbox flex-1': true, 'has-error': errors.has(item.rate_name)}">
                                                <input class="cro-qbox__input font-14px" :name="item.rate_name" v-model="item.rate" v-validate="{required: true}" type="text" placeholder="请选择百分比" style="background-color: transparent;" readonly="readonly" @@click="onQ12Picker($event,item)" />
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </article>
                </div>
                <footer class="cre-footer">
                    <a class="btn btn-primary cre-submit a-btn" href="javascript: void(0);" @@click="onSubmit">下一页</a>
                    <div style="height: 38px;"></div>
                </footer>
            </div>
        </div>
    </div>
</div>