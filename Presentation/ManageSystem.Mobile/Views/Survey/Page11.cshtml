
@{
    ViewBag.Title = "问卷";
    Layout = "~/Views/Shared/_SurveyLayout.cshtml";
    string _version = DateTime.Now.Ticks.ToString();
}

@section Styles{
    <link href="~/Content/Css/Survey/Page11.css?v=@_version" rel="stylesheet" />
}
@section Scripts{
    <script src="~/Content/Scripts/Survey/Page11.js?v=@_version"></script>
}

<div id="app" v-locak>
    <input id="hidden_id" name="hidden_id" ref="hidden_id" type="hidden" hidden="hidden" value="@ViewContext.RouteData.Values["id"]" />
    <div id="page" class="swiper-container">
        <div class="swiper-wrapper">
            <div class="swiper-slide slidescroll">
                <div class="survey-body">
                    <article class="cro-page">
                        <div class="cro-qbox">
                            <div class="cro-qbox-title" v-html="Q21.title"></div>
                            <div :class="{'cro-qbox-awbox': true, 'has-error': errors.has(Q21.name)}">
                                <input class="cro-qbox__input" :name="Q21.name" v-model="Q21.answer" v-validate="{required: true, decimal: 0}" type="tel" :placeholder="Q21.placeholder" />
                            </div>
                        </div>
                        <div class="cro-qbox">
                            <div class="cro-qbox-title" v-html="Q22.title"></div>
                            <div :class="{'cro-qbox-awbox': true, 'has-error': errors.has(Q22.name)}">
                                <input class="cro-qbox__input" :name="Q22.name" v-model="Q22.answer" v-validate="{required: true, decimal: 0}" type="tel" :placeholder="Q21.placeholder" />
                            </div>
                        </div>
                    </article>
                </div>
                <footer class="cre-footer">
                    <a class="btn btn-primary cre-submit a-btn" href="javascript: void(0);" @@click="onSubmit">下一页</a>
                </footer>
            </div>
        </div>
    </div>
</div>