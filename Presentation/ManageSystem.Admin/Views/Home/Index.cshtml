@model ManageSystem.Admin.Models.Users.UserinfoModel
@using ManageSystem.Admin.Models.Log

@{
    ViewBag.Title = "首页";
}

@section HeadSection {
}

@section Breadcrumb{
    @{Html.RenderAction("Breadcrumb", "Shared", new Breadcrumb { MenuName = "", SecondName = "", LocationName = "后台首页" }); }
}

<div class="page-body">
    <div class="row">
        <div class="col-lg-4 col-sm-12 col-xs-12">
         @{Html.RenderAction("_LoginActionLog", "Home");}
        </div>
        <div class="col-lg-8 col-sm-12 col-xs-12">

            <div class="row">
                <div class="col-lg-12">
                    @{Html.RenderAction("_WeatherMessage", "Home");}
                </div>
            </div>
        </div>
    </div>
</div>