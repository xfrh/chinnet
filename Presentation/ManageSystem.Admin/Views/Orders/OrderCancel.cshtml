@model ManageSystem.Admin.Models.Orders.OrderModel
@{
    Layout = "~/Views/Shared/_OpenLayout.cshtml";
}

<div class="page-body">
    <div class="row manage-page">
        <div class="col-xs-12 col-md-12">
            <div class="widget">
                <div class="widget-header bordered-bottom bordered-yellow manage-title-box">
                    <span class="widget-caption manage-title"><i class="glyphicon glyphicon-th "></i>@ViewBag.Title </span>
                    <div class="widget-buttons">
                        <a href="#" data-toggle="maximize"><i class="fa fa-expand"></i></a>
                        <a href="#" data-toggle="collapse"><i class="fa fa-minus"></i></a>
                    </div>
                </div>

                <div class="widget-body">
                    <div role="grid" id="searchable_wrapper" class="dataTables_wrapper form-inline">

                        <div id="searchable_filter" class="dataTables_filter manage-but-box">
                            <div style="width: 100%;">
                                <a class="btn btn-purple" href="javascript:Save()">确认取消</a>
                            </div>
                            <div style="width: 100%;">
                                <div class="add-content ">
                                    <div class="k-content k-state-active" id="customer-edit-1" role="tabpanel" aria-expanded="true" style="display: block;">

                                        <table class="adminContent">
                                            <tr class="insert-time-tr">
                                                <td class="adminTitle" style="width:30%;">@Html.NopLabelFor(m => m.SN)：</td>
                                                <td class="adminData" style="width:60%;">
                                                @Model.SN
                                                </td>
                                            </tr>
                                            <tr class="insert-time-tr">
                                                <td class="adminTitle">@Html.NopLabelFor(m => m.Amount)：</td>
                                                <td class="adminData">
                                                    @Model.Amount
                                                </td>
                                            </tr>
                                            <tr>
                                                <td class="adminTitle">@Html.NopLabelFor(m => m.Describe)：</td>
                                                <td class="adminData">
                                                    @Html.TextAreaFor(model => model.Describe, new { @class = "form-control txt", cols = "30", rows = "3" })
                                                </td>
                                            </tr>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="table table-striped table-bordered dataTable no-footer table-select" id="ManageTableDiv" aria-describedby="editabledatatable_info">
                            <div id="data-grid"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>




<script type="text/javascript">

    var index = parent.layer.getFrameIndex(window.name); //获取窗口索引


    //选择文章以后的回调函数
    function Save() {

        $.ajax({
            type: "POST",
            url: "@Url.Action("OrderCancel", "Orders")",
            data: {
                Id:'@Model.Id.ToString()',
                Describe: $("#Describe").val()
            },
            dataType: "json",
            success: function (data) {
                if (data == "" || data == null) return;
        
                alert(data.message);
                if(data.status == "true")
                {
                    parent.ReloadPage();
                    parent.layer.close(index);
                }
            }
        });
    }
</script>
